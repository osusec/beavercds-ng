---
crds:
  # install the cert-manager CRDs as part of the chart
  enabled: true
  # also delete them when the chart is deleted
  # (the chart errors when installing if its CRDs are still around)
  keep: false
